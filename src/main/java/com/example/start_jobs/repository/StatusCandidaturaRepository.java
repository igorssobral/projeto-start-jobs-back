package com.example.start_jobs.repository;

import com.example.start_jobs.entity.StatusCandidatura;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StatusCandidaturaRepository extends JpaRepository<StatusCandidatura, Integer> { }
